ModelPickerScreen {
    align: center middle;
    background: $background 60%;
}

#model-picker {
    width: 72;
    border: round $primary;
    background: $surface;
    padding: 1;
    height: 24;
}

#model-query {
    margin-bottom: 1;
}

#model-options {
    height: 1fr;
}
