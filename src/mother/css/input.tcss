#prompt-area {
    height: auto;
    border-top: wide $primary;
}

#prompt-input {
    height: 6;
    border: none;
}

#slash-complete,
#slash-argument-complete,
#model-complete {
    margin: 0 1;
    max-height: 8;
    border: round $primary;
    background: $surface;
}
